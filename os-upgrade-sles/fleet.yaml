# fleet.yaml
# Deploys resources to the default SUC namespace
defaultNamespace: cattle-system

# In common scenarios, this targets all downstream clusters. 
# Adjust the matchLabels to target specific clusters if needed.
targetCustomizations:
  - name: all-clusters
    clusterSelector:
      matchLabels: {}